<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Events - Cyber Phoenix</title>

        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Chakra+Petch:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Kode+Mono:wght@400..700&family=Space+Grotesk:wght@300..700&display=swap"
            rel="stylesheet" />

        <link rel="stylesheet" href="/css/global.css" />
        <link rel="stylesheet" href="/css/events.css" />
        <link rel="shortcut icon" href="/assets/logo.ico" type="image/x-icon" />

         <!-- Google Analytics -->
        <script async src="https://www.googletagmanager.com/gtag/js?id=G-RKGR9PSRNT"></script>
        <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-RKGR9PSRNT');
        </script>
    </head>

    <body>
        <header><%- include("../includes/navbar.ejs") %></header>

        <main>
            <section class="section1">
                <div class="section1-head vertical-flex-center">
                    <h1 class="vertical-flex-center">
                        <span>EXPERIENCE</span>
                        <span>OUR WILD CYBER</span>
                        <span>MAGIC</span>
                    </h1>

                    <h3 class="vertical-flex-center">
                        <span>Experience the wild array of</span
                        ><span> events that make us </span
                        ><span>stand out</span>
                    </h3>
                </div>
            </section>

            <section class="section2">
                <% for (const event of events) {%>
                <div class="event">
                    <h1 class="event-date date1">
                        <span class="vertical-reveal"
                            ><% if (!event.date) { %> To Be Revealed <% } else {
                            %><%= event.date.toLocaleString("en-US", { day:
                            'numeric', month: 'long', year: 'numeric' }) %> <% }
                            %></span
                        >
                    </h1>

                    <div class="event-info">
                        <h1 class="event-title"><%= event.title %></h1>

                        <div class="event-keywords">
                            <div class="keywords">
                                <% for( let i = 0; i <
                                event.keywords.length/2;i++ ) { %>
                                <h5><%= event.keywords[i] %></h5>
                                <% } %>
                            </div>
                            <div class="keywords">
                                <% for( let i = event.keywords.length/2; i <
                                event.keywords.length;i++ ) { %>
                                <h5><%= event.keywords[i] %></h5>
                                <% } %>
                            </div>
                        </div>

                        <% if (event.registerLink) { %><button>
                            Register Now</button
                        ><% } %>
                    </div>
                </div>
                <% } %>
            </section>
        </main>

        <%- include("../includes/footer.ejs") %>

        <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.7/dist/gsap.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.7/dist/ScrollTrigger.min.js"></script>
        <script src="/js/events.js"></script>
        <script src="/js/navbar.js"></script>
    </body>
</html>
